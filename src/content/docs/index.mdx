---
title: QuickTurn CBT
description: Your Centralized CBT Resource
---

import BmcPopup from '../../components/BmcPopup';

<style>{`
  /* Pulsing effect for the BMC button */
  @keyframes pulseBmcOnly {
    0% {
      transform: scale(1);
      filter: drop-shadow(0 0 0px rgba(255, 221, 0, 0));
    }
    50% {
      transform: scale(1.06);
      filter: drop-shadow(0 0 16px rgba(255, 221, 0, 0.9));
    }
    100% {
      transform: scale(1);
      filter: drop-shadow(0 0 0px rgba(255, 221, 0, 0));
    }
  }

  /* Breathing (scale) animation */
  @keyframes bannerBreathe {
    0% { transform: scale(1); }
    50% { transform: scale(1.05); }
    100% { transform: scale(1); }
  }

  /* Gentle clockwise/counterclockwise rotation */
  @keyframes bannerRotate {
    0% { transform: rotate(0deg); }
    25% { transform: rotate(3deg); }
    50% { transform: rotate(0deg); }
    75% { transform: rotate(-3deg); }
    100% { transform: rotate(0deg); }
  }

  .banner-outer {
    animation: bannerRotate 30s linear infinite;
    transform-origin: center;
    display: flex;
    justify-content: center;
    margin-top: 2rem;
  }

  .banner-inner {
    animation: bannerBreathe 8s ease-in-out infinite;
    display: block;
    max-width: 100%;
    border-radius: 0.75rem;
    box-shadow: 0 10px 25px rgba(0,0,0,0);
    overflow: hidden;
  }
`}</style>

<div className="banner-outer">
  <img
    src="/images/banner.png"
    alt="QuickTurn CBT Banner"
    className="banner-inner w-full max-w-4xl"
  />
</div>

<div
  style={{
    textAlign: 'center',
    fontWeight: '500',
    marginTop: '2rem',
  }}
>
  This is an <strong>UNOFFICIAL</strong> CBT Study Resource. Any similarities between official CBT
  content and this Study Resource are purely coincidental.
</div>

---

<div
  style={{
    marginTop: '3rem',
    textAlign: 'center',
    display: 'flex',
    justifyContent: 'center',
  }}
>
  <a
    href="https://www.buymeacoffee.com/qtcbt"
    target="_blank"
    rel="noopener noreferrer"
    style={{
      display: 'inline-block',
    }}
  >
    <img
      src="https://img.buymeacoffee.com/button-api/?text=Buy me a coffee&emoji=☕&slug=qtcbt&button_colour=FFDD00&font_colour=000000&font_family=Inter&outline_colour=000000&coffee_colour=ffffff"
      alt="Buy Me a Coffee"
      style={{
        height: '44px',
        maxWidth: '100%',
        animation: 'pulseBmcOnly 3.5s ease-in-out infinite',
      }}
    />
  </a>
</div>
